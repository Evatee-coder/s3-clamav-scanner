# s3-clamav-scanner
S3 ClamAV Scanner  A production-style, event-driven file scanning pipeline that scans every file uploaded to S3 for viruses and malware before it reaches downstream processing — built around ClamAV, fully containerized, and designed to scale to zero when idle.
