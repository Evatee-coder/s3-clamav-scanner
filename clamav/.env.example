# Copy this file to .env (already gitignored) and fill in your own values,
# or export these as real environment variables before running main2.py.

LANDING_BUCKET=your-landing-bucket-name
CLEAN_BUCKET=your-clean-bucket-name
NOTIFICATION_QUEUE=scan-notification-queue
